@using Core.Domain.Enums;
@model PackageModel

@{
    ViewData["Title"] = "Pakket aanmaken";
    ViewBag.CanServeWarm = false;

    Console.WriteLine(Html.GetEnumSelectList<MealtypeEnum>().Where(x => !x.Value.Equals(4)));
}

<form asp-action="CreatePackage" asp-controller="Package" method="post">
    <div class="form-group">
        <label asp-for="Name"></label>
        <div><span asp-validation-for="Name" class="text-danger"></span></div>
        <input asp-for="Name" class="form-control" />
    </div>

    <div class="form-group">
        <label asp-for="Products"></label>
        <span asp-validation-for="Products" class="text-danger"></span>
    
        @foreach (var item in Model.AvailableProducts!)
        {
            <div class="checkbox">
                <label>
                    <input type="checkbox"
                       name="SelectedProducts"
                       value="@item.Value"
                       @if(Model.SelectedProducts!.Contains(item.Value))
                       {
                        <text> checked </text>
                       } /> @item.Text
                </label>
            </div>
        }
    </div>
    <div class="form-group">
        <label asp-for="PickUpTime"></label>
        <div><span asp-validation-for="PickUpTime" class="text-danger"></span></div>
        <input asp-for="PickUpTime" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="LatestPickUpTime"></label>
        <div><span asp-validation-for="LatestPickUpTime" class="text-danger"></span></div>
        <input asp-for="LatestPickUpTime" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="Price"></label>
        <div><span asp-validation-for="Price" class="text-danger"></span></div>
        <input asp-for="Price" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="MealType"></label>
        <div><span asp-validation-for="MealType" class="text-danger"></span></div>
        <select asp-for="MealType" class="form-control" asp-items="ViewBag.CanServeWarm ? Html.GetEnumSelectList<MealtypeEnum>() : Html.GetEnumSelectList<MealtypeEnum>().Where(x => !x.Value.Equals(4))">
            <option>Selecteer</option>
        </select>
    </div>
    <button class="btn btn-primary" style="margin-top:15px;" type="submit">Pakket aanmaken</button>
    </div>
</form>